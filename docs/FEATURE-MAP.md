# Kapot Tracker — карта функціоналу та беклог

Знімок стану на 2026-07-14. Джерело — мультиагентний аудит коду (7 доменів) + дослідження конкурентів (Drivvo, Fuelio, Simply Auto, aCar/Fuelly, Spritmonitor). Усі пропозиції звірені з живим кодом і seeded-даними (`backend/kapot_tracker.db`: 1 авто — VW Golf VII Variant 2016, 240 054 км, 7 інтервалів, 19 записів, 0 заправок).

> Затверджена ітерація редагування (сторінка запису `/logbook/:id`, редагування, фото-вкладення, пошук, швидкий одометр, дублювання, Modal/ConfirmDialog) вважається зафіксованою і в беклозі не дублюється — позначається як «⏳ вже у затвердженій ітерації».

---

## Що вже є

**Журнал (logbook)**
- Форма 4 типів записів (`?type=` deep-links, FAB), розумні префіли: дата = сьогодні, одометр з `car.current_odometer`, «повний бак» = true.
- Авто-математика заправки: будь-які 2 з 3 (літри/ціна/сума) обчислюють третє, з толерантністю до коми (`refuelMath.js`).
- OCR чека прямо у формі заправки: префіл лише незайманих полів (`editedDuringScanRef`), toast з підсумком.
- Backend: `PATCH /logs/{id}` повністю реалізований (зі зміною типу і detail-рядків) — API для затвердженої ітерації вже готове (`logs.py:168-268`); пагінація limit/offset + total підтримується сервером.
- Одометр авто рухається тільки вперед; валідація з українськими повідомленнями.

**Гараж та сервісні інтервали**
- Мультиавто з глобальним селектором, активне авто в localStorage.
- Двигун статусів: `due_odometer`, `predicted_due_date` (мінімум із км-темпу та календаря), `health_pct`, пороги due_soon/overdue (`services/intervals.py`).
- Інтервали за км І/АБО днями — date-only інтервали вже валідні за схемою.
- Щоденний Telegram-цикл нагадувань з 7-денним cooldown і тижневим одометр-nudge.

**Паливо та аналітика**
- Повноцінний full-to-full двигун витрати: часткові заправки акумулюються в наступний повний сегмент (`services/fuel.py`).
- Аналітика: тотали, розбивка за 4 типами, 12-місячний stacked-графік, лінія витрати.
- Прогноз (`services/forecast.py`): місячний темп км, середні витрати, проєкція поточного місяця, майбутні ТО з оцінкою вартості за медіаною (український keyword-matching).
- PDF-звіт сервісної історії (reportlab).

**Telegram-бот**
- Окремий контейнер, long-polling; лінкування через короткоживучий purpose-scoped JWT-код.
- `/status` (топ-3 інтервали за health), оновлення одометра голим числом (forward-only), швидкі витрати «назва сума», inline-вибір авто.
- Цикл нагадувань з ізоляцією помилок per-user.

**OCR та документи**
- Зріло загартований парсер чеків АЗС: 12 українських брендів (кирилиця+латиниця), fold латинських lookalike (CYMA→СУМА), rescue суми через liters×price consistency band, ~30 тестів включно з verbatim-дампом реального чека OKKO (`services/ocr.py`, `test_ocr.py`).
- `POST /ocr/scan`: JWT, ліміт 10 МБ, обробка відсутнього tesseract (503).
- `pypdf` вже у залежностях (використовується лише в тестах звіту).

**PWA та мобільний UX**
- vite-plugin-pwa (generateSW, autoUpdate), standalone, mobile-first shell: max-w-md, sticky header, нижній таб-бар з FAB та safe-area.
- Правильні мобільні клавіатури (`inputMode` numeric/decimal), `capture="environment"` для камери.
- nginx PWA-aware: no-cache для sw.js/manifest, SPA fallback, 12 МБ body для OCR.

**Дані та безпека**
- bcrypt + HS256 JWT; email lowercased; purpose-claim захист (link-код не працює як access-токен).
- `.env` та `*.db` в gitignore; секрети не в git; auth покритий тестами.
- Postgres 16 у compose з named volume; SQLite за замовчуванням у dev.

---

## Наші переваги над конкурентами

Порівняння з Drivvo, Fuelio, Simply Auto, aCar/Fuelly, Spritmonitor (2025–2026). Kapot вже покриває ~70% очікуваного baseline, а в кількох місцях — попереду всіх шістьох:

1. **Українськомовний OCR чеків** — лише Spritmonitor має скан рахунків узагалі, і жоден не підтримує українську.
2. **Telegram-бот** (логування, /status, нагадування) — жоден із шести не має месенджер-вводу (SMS у Fuelly — legacy US-only), а для українських звичок це ідеальний канал.
3. **Прогноз уперед** (проєкція витрат місяця + оцінка вартості майбутніх ТО) — усі шість малюють лише історію.
4. **PDF «сервісний паспорт»** — високодовірчий артефакт при продажу авто з ЄС-імпортом.
5. **Серверна PWA = безкоштовна синхронізація між пристроями і веб-доступ** — Drivvo та Simply Auto продають це як Pro-підписку.
6. **Явне відстеження гарантії ремонтів** (місяці/км) — рідкість навіть серед платних конкурентів.
7. **Без реклами, без підписки, свої дані** — болюча тема в відгуках aCar/Fuelly після переходу на підписку.

Найгучніші відсутні table-stakes: CSV експорт/імпорт, категорії витрат, офлайн-ввід. Найбільші локальні (UA) можливості: документи з ОСЦПВ-дедлайнами, контекст цін на пальне.

---

## Діри та тертя

**Журнал**
- Нотатки — однорядковий `<Input>`, а реальні seeded-нотатки — 199–382 символи з номерами накладних; у списку обрізаються до 1 рядка, для витрат уся нотатка стає заголовком (`AddEntry.jsx:516`, `LogTimelineItem.jsx:26,50`).
- АЗС передруковується вручну щоразу, ціна за літр щоразу з нуля — форма з найбільшою частотою не пам'ятає нічого.
- Нуль sanity-перевірок одометр/дата: у seeded-даних вже є суперечність (лог 15: 2025-06-15 @ 236 000 км проти лог 18: 2025-07-18 @ 224 900 км); застарілий префіл одометра загрожує сегментам full-to-full.
- Список прибитий до перших 50 записів без load-more (`carStore.js:67`), видалення безповоротне через `window.confirm`.
- Decimal-кома напівпідтримана: парсер розуміє «54,99», але `type="number"` блокує ввід коми — а на українських клавіатурах десяткова клавіша це кома.
- Тип «витрата» без структури: ОСЦПВ, мийка, паркування, штрафи — усе в довільному тексті нотатки.

**Гараж та інтервали**
- Завершити інтервал з UI неможливо: `updateInterval` — мертвий код, `routers/logs.py` жодного разу не торкається `ServiceInterval` → після заміни оливи: видалити інтервал + створити заново + окремо записати ТО.
- `avg_daily_km` — lifetime-середнє від першого до останнього запису: seeded-історія з 2016 (німецький попередній власник) викривлює прогнози до 2×; без пояснення і без override.
- Профіль авто занадто тонкий для ЄС-імпорту: нема VIN (потрібен щотижня для exist.ua/autodoc), номера, дат ОСЦПВ/техогляду/зеленої карти.
- Пресети ігнорують `fuel_type` (дизель без DPF/EGR-пунктів, ГБО без жодного LPG-пункту), дублюються при повторному натисканні, якорені до «сьогодні» замість реальної дати останнього ТО.
- Жодного трекінгу шин/сезонної заміни — двічі-на-рік обов'язковість для кожного українського водія.

**Паливо та аналітика**
- Dashboard із 0 заправок — мертвий вантаж: 2 з 3 плиток назавжди «—» без CTA (в Analytics хороший empty-state, на головному екрані — ні).
- `price_per_liter` і `gas_station` збираються на кожній заправці, але ніде не аналізуються (ні тренду ціни, ні порівняння брендів).
- Нема `fuel_type` на заправці — ГБО-авто (LPG+бензин) неможливо змоделювати; ретрофіт болючішає з кожним новим рядком.
- ₴/км-плитка mislabeled (рахує лише пальне в межах виміряних сегментів), місячні км/₴-км непорахунковані в UI, X-вісь графіка витрати категоріальна (розриви в 2 тижні і 3 місяці виглядають однаково).

**Telegram-бот**
- Дві найчастіші події — заправки і ТО — бот логувати не вміє взагалі.
- Тиха пастка: будь-який «слово число» стає збереженою витратою без підтвердження і без undo — «240 054» (одометр з пробілом) зберігається як витрата «240» на 54 грн; природне «мийка 300 грн» — навпаки, відхиляється.
- Фото чека (яке веб-OCR вже розуміє) — повна тиша: хендлер лише на `F.text`.
- Втома від нагадувань без виходу: гальмівна рідина seeded-авто прострочена на 589 днів і нагадується кожні 7 днів вічно; на повідомленнях нема кнопок «виконано»/«відкласти».
- Розклад прив'язаний до старту процесу (+60 с, потім +24 год) — після деплою нагадування дрейфують хоч на 3:00 ночі; нема `set_my_commands`.

**OCR та документи**
- PDF жорстко відбивається 415-ю: реальні PDF-рахунки від «Алекс Со» відсканувати неможливо, хоча `pypdf` вже в залежностях.
- OCR допомагає лише формі заправки; ТО/ремонти (10+ позицій, запчастини/роботи) — без допомоги.
- Джерело викидається: ні зображення чека, ні `raw_text` ніде не зберігаються — сумнівне значення не перевірити, чек фотографувати двічі.
- Нема бібліотеки документів: техпаспорт, поліс ОСЦПВ, зелена карта, митні папери — без домівки і без зв'язку з нагадуваннями.
- Рукописні наряди СТО — за межами tesseract (укр. handwriting traineddata не існує).

**PWA та мобільний UX**
- Іконка на iOS зламана: iOS ігнорує SVG `apple-touch-icon` → буквена плитка на домашньому екрані єдиного реального користувача щодня; manifest `lang: "en"` для повністю української аппки.
- Офлайн на заправці — найгірший сценарій: shell вантажиться, але кожен API-виклик падає у «Не вдалося завантажити…», а заповнена форма заправки втрачає дані при виході з роуту (жодного стану поза компонентом, жодної черги — попри оригінальний spec з IndexedDB).
- Тільки темна тема з hardcoded slate-класами — погана читабельність на сонці біля колонки OKKO в липневий полудень.
- Нема refetch-on-focus і pull-to-refresh: standalone PWA відновлюється зі вчорашніми цифрами; SW оновлюється мовчки.
- Onboarding розкиданий: пресети інтервалів і Telegram-лінк знаходяться лише прокруткою Гаража до кінця.

**Дані, акаунт, платформа**
- Уся реальна історія 238-тис.-км Golf живе в одному gitignored SQLite-файлі на десктопі — **і його вже одного разу випадково видаляли**. Нуль механізмів бекапу в репозиторії.
- Нема machine-readable експорту/імпорту — заблокована навіть міграція dev-SQLite → Postgres/VPS, якої потребує сам проєкт.
- Забутий пароль = дані назавжди недоступні: нема reset, нема change-password, email не верифікується.
- `/api/auth/token` без rate limit, `/register` відкритий; дефолтні `SECRET_KEY` («change-me-in-production») стартують мовчки — хто знає дефолт, той карбує валідні JWT.
- Конфіг токена суперечить сам собі: 30 днів у dev проти 60 хвилин у compose (щогодинний релогін на телефоні).
- Продакшн-шляху нема: plain HTTP, а PWA поза localhost вимагає HTTPS — задеплоєну аппку зараз неможливо встановити.

---

## Беклог із пріоритетами

70 пропозицій від 8 агентів → **61 після дедуплікації** (9 злиттів позначені в колонці «Домен»). Сортування в кожній таблиці: за зусиллям (S→M→L), всередині — за впливом. ⏳ = залежність від затвердженої ітерації редагування.

### P1 — зробити наступним (22)

| Фіча | Домен | Чому | Оцінка | Залежності |
|---|---|---|---|---|
| Автоматичний бекап БД з ротацією + off-box копія через Telegram-бот | Дані/безпека | Інцидент втрати даних уже стався; ~50-рядковий скрипт (sqlite3 `.backup`/pg_dump) + `sendDocument` у власний чат — нуль нової інфри | S | — |
| Категорії витрат + розбивка не-паливних витрат в аналітиці | Конкуренти + Журнал (злито) | Table stakes навіть у безкоштовному Drivvo; чіпи в формі (ОСЦПВ, мийка, паркування, штраф, шини) + міграція category + keyword-мапа в боті | S | — |
| Compliance-інтервали «за датою»: пресети ОСЦПВ, техогляд, зелена карта | Гараж | Прострочене ОСЦПВ = штраф; модель уже підтримує days-only — лишились режим форми «за датою» + група пресетів, нагадування їдуть існуючим Telegram-циклом безкоштовно | S | — |
| Підтвердження перед збереженням швидкої витрати в боті | Telegram | Вбиває найгіршу живу пастку: «240 054» → сміттєва витрата без undo; одна inline-клавіатура на існуючому шляху | S | — |
| Inline-кнопки «✅ Виконано» / «🔕 Відкласти 30 дн.» на нагадуваннях | Telegram + Гараж (злито) | Нагадування про гальмівну рідину інакше нагадуватиме кожні 7 днів вічно; snooze = bump `last_notified_at`, done = штамп дат/одометра | S | Повна транзакційна версія — через ендпоінт `/complete` |
| Санітарні попередження одометр/дата в AddEntry (non-blocking) | Журнал | Seeded-дані вже містять суперечність дата/одометр; попередження + підказка «останній запис: 238 150 км» захищає full-to-full математику | S | — |
| Пам'ять останніх значень: АЗС-чіпи + префіл останньої ціни | Журнал | Щотижня передруковуються ті самі «OKKO»/ціна; скорочує ручну заправку до: поправити одометр, ввести літри, тапнути АЗС | S | — |
| Багаторядкові нотатки: textarea + 2–3-рядковий clamp у списку | Журнал | Реальні нотатки 200–380 символів з номерами накладних зараз нечитабельні й болючі у вводі | S | Повний текст — на ⏳ сторінці запису |
| Фікс avg_daily_km: вікно ~120 днів + ручний override + пояснення | Гараж | Lifetime-середнє з 2016 року бреше прогнозам до 2×; прямо покращує predicted_due_date і таймінг нагадувань цього ж місяця | S | — |
| Onboarding першої заправки: плитки «—» стають CTA | Паливо | 0 заправок у БД — це перше, що відчує власник; лінк на `/add?type=refuel` + підказка «витрата рахується з другого повного бака» | S | — |
| Графік ціни за літр | Паливо | Дані вже на кожній заправці; найдешевший новий інсайт на рядок коду | S | — |
| Справжні іконки + фікси manifest (PNG 192/512 + maskable, apple-touch-icon 180px, lang: uk) | PWA | Власник щодня бачить зламану буквену плитку на iPhone; чиста робота з асетами | S | — |
| Офлайн-індикатор + NetworkFirst-кеш GET /api | PWA | Одногодинний win: офлайн зі «стіни помилок» перетворюється на робочий read-only застосунок | S | — |
| Збереження сканованого чека як вкладення запису + raw_text | OCR | Одне фото робить обидві роботи (скан + вкладення); кожен минулий скан стає аудитованим | S | ⏳ фото-вкладення + сторінка запису |
| Повний експорт/імпорт: JSON усього акаунта + CSV (мапінг Fuelio/Drivvo) | Дані + Конкуренти + Паливо (злито 3 пропозиції) | Одночасно юзерський бекап, відповідь на data ownership, відсутній міграційний шлях SQLite→Postgres/VPS і table stakes усіх шести конкурентів | M | — |
| Скидання пароля через прив'язаний Telegram (+ change-password) | Дані/безпека | Другий шлях тотальної втрати даних; purpose-scoped JWT-патерн, chat_id і надсилання ботом уже існують — незвично дешево | M | Telegram-лінк (вже є) |
| One-tap «Виконано»: POST `/intervals/{id}/complete` — запис ТО + просування інтервалу транзакційно | Гараж | Закриває найбільшу діру домену: зараз інтервал просувається лише через delete+recreate у двох роз'єднаних місцях | M | ⏳ Modal/ConfirmDialog |
| Заправка текстом у боті: «заправка 1800 41.5л» | Telegram | Найчастіша подія недоступна боту взагалі; живить full-to-full двигун — серцевину цінності застосунку | M | — |
| Фото чека → OCR → confirm-картка в боті | Telegram | OCR-пайплайн уже існує, але на заправці відкритий Telegram, а не PWA; tesseract у `asyncio.to_thread`, webhook не потрібен | M | Заправка текстом (спільна confirm-картка) |
| PDF у `/ocr/scan`: спершу текстовий шар pypdf, растровий fallback | OCR | Реальні PDF-рахунки «Алекс Со» сьогодні отримують 415; текстовий шар чистіший за будь-яке фото, pypdf уже в залежностях | M | — |
| Витрата л/100км прямо на рядках заправок + toast після збереження | Журнал | Найбільш мотивуюча цифра для власника дизеля з 238 тис. км (DPF/форсунки) зараз похована в іншому екрані; ляже саме коли формується звичка | M | — |
| Офлайн-черга заправок + чернетка форми (IndexedDB/Dexie) | PWA + Конкуренти (злито) | Найцінніший офлайн-сценарій: заправка без сигналу йде в outbox і синкається на `online`; чернетка рятує введене. Усі мобільні конкуренти offline-first | L | Офлайн-індикатор + кеш (узгоджений меседжинг) |

### P2 — наступна хвиля (27)

| Фіча | Домен | Чому | Оцінка | Залежності |
|---|---|---|---|---|
| Undo після видалення замість «неможливо скасувати» | Журнал | Кошик завжди видимий на кожному рядку; кеш payload + «Скасувати» в Toast на ~5 с | S | ⏳ Toast/ConfirmDialog |
| «Зберегти і додати ще» (другий сабміт) | Журнал | 19 seeded-записів доводять, що бекфіл пачками трапляється; N записів за один захід замість N навігаційних кіл | S | — |
| Load-more пагінація + місячні заголовки в журналі | Журнал | Backend уже вміє offset; при ~30 записах/рік стіна з 50 — за сезон | S | — |
| Фікс decimal-коми: type="text" + inputMode="decimal" | Журнал | Парсер уже розуміє кому, але `type="number"` її блокує — найтихіший збій у найчастіших полях | S | — |
| VIN + номерний знак на Car (+ у PDF-звіті) | Гараж | Для ЄС-імпорту VIN вводиться в каталоги запчастин щотижня; номер у шапці PDF робить документ корисним на СТО і при продажу | S | — |
| Шини: парні сезонні date-інтервали + нотатка про комплект | Гараж | Зимова заміна за ~3 місяці; їде існуючим конвеєром нагадувань, без нової підсистеми | S | — |
| Автокомпліт АЗС + аналітика по брендах (середня ціна, витрата) | Паливо + Конкуренти (злито) | Фіксить дрейф вільного тексту в місці вводу і відповідає на «OKKO чи WOG» даними, які вже вводяться; головна маркетингова фіча Simply Auto майже задарма | S | Графік ціни за літр (спільний payload) |
| Об'єм бака на Car + оцінка запасу ходу | Паливо | «≈ 900 км на повному баку» для 50-літрового 1.6 TDI + перехоплення одруків (літри > бака) | S | Хоча б один виміряний сегмент витрати |
| /report — PDF-звіт документом у Telegram | Telegram | `build_report` уже є; корисно рівно там, де треба: на СТО або при продажу | S | — |
| Fallback-хендлери + set_my_commands | Telegram | Автокомпліт /status; фото/голос/невідомі команди перестають мовчати або падати в парсер витрат | S | Частково поглинається фото-OCR у боті |
| Raw-text панель + збір виправлень + підсвітка OCR-заповнених полів | OCR + Журнал (злито) | Diff «розпізнано → збережено» = самонарощуваний регресійний корпус у форматі, який test_ocr.py уже споживає | S | — |
| Onboarding-чекліст на Dashboard (авто → пресети → Telegram → перша заправка) | PWA | Три кроки, що роблять аппку цінною, зараз розкидані й неявні; без нового backend | S | — |
| Клавіатурно-resume поліш: enterkeyhint, autofocus, refetch on visibilitychange, toast оновлення SW | PWA | Пакет дрібних щоденних відчуттів; яскравіше за все — свіжі дані при відновленні standalone-PWA | S | — |
| Rate limit на логін + відмова стартувати з дефолтним SECRET_KEY (+ прибрати 8000:8000) | Дані/безпека | Два дешеві укріплення перед виходом в інтернет; single-process — Redis не потрібен | S | — |
| REGISTRATION_ENABLED (у prod вимкнено) | Дані/безпека | Персональний інстанс на публічному домені не має приймати чужі реєстрації | S | — |
| Серверні пресети інтервалів за fuel_type + дедуп | Гараж | Дизель отримує EGR/DPF, бензин — свічки, ГБО — свій набір; ідемпотентність лагодить баг дублювання | M | — |
| fuel_type на заправці (ГБО-ready) з per-fuel сегментами | Паливо + Конкуренти (злито; конфлікт P2↔P3, див. нижче) | Міграція тривіальна зараз і болюча після накопичення історії; ГБО — найпоширеніша конфігурація українських бензинових авто | M | Краще до накопичення реальних заправок |
| Км/місяць + ₴/км по місяцях (+ чесна назва плитки «Пальне, ₴/км») | Паливо | Одометр уже в кожному записі — місячні дельти км обчислювані; плитка зараз мітить себе повною вартістю км і бреше | M | — |
| Тижневий дайджест + прив'язка розкладу до фіксованої години Europe/Kyiv | Telegram | Змушує зробити фікс планувальника, від якого виграють усі нагадування (зараз дрейфують на 3:00 після деплоїв) | M | — |
| Парсер друкованих накладних/нарядів: позиції → items + parts/labor | OCR | ТО — найнудніше у ручному вводі; чесний скоуп: лише друковані/PDF-text-layer (рукопис — до LLM-fallback) | M | PDF-підтримка (текстовий шар) |
| Бібліотека документів авто з expiry → date-інтервали (ОСЦПВ, зелена карта, техогляд, фото поліса) | OCR + Конкуренти (злито) | Прострочене ОСЦПВ = штраф + повна особиста відповідальність; фото поліса за один тап при зупинці поліцією; нагадування T-14/T-3 їдуть існуючою машинерією | M | ⏳ фото-вкладення |
| Opt-in LLM-vision fallback для рукопису і брудних документів | OCR | Рукописні наряди СТО категорично поза tesseract; ~$0.01–0.03/скан, env-gated + явна кнопка «Розпізнати через AI», tesseract лишається дефолтом | M | — |
| Світла тема, читабельна на сонці (auto/manual) | PWA | Головний момент використання — біля колонки в липневий полудень; darkMode:'class' + механічний прохід ~10 файлів | M | — |
| Pull-to-refresh на Dashboard і Logbook | PWA | У standalone нема кнопки reload; ~60 рядків touch-логіки без залежностей | M | Resume-поліш (спільний refetch) |
| VPS-runbook + compose.prod з Caddy HTTPS | Дані/безпека | PWA поза localhost не встановлюється без HTTPS — деплой-стори зараз функціонально зламана | M | Бекап-скрипт (день 1 на VPS вже з бекапом) |
| Session sanity: єдиний 30-денний токен + token_version («вийти всюди») | Дані/безпека | Зараз 30 днів у dev проти 60 хв у compose — обидві крайності неправильні; token_version = kill switch загубленого телефона без refresh-механіки | M | — |
| Щоденний фід цін на пальне UA + «ви заплатили vs ринок» | Конкуренти (телеграм-агент проти — див. конфлікти) | Публічні брендові ціни (index.minfin.com.ua, auto.ria.com/uk/toplivo); рідкісна фіча, яку жоден конкурент не робить локально для України | M | Бренд-аналітика АЗС (спільна нормалізація) |

### P3 — коли дійдуть руки (12)

| Фіча | Домен | Чому | Оцінка | Залежності |
|---|---|---|---|---|
| Персистентні кастомні пункти ТО (localStorage/prefs) | Журнал | «Фільтр Mahle KX386» зараз випаровується при unmount і передруковується на кожному сервісі | S | — |
| Статус-крапка найгіршого інтервалу на картці авто в Гаражі | Гараж | Прострочене ОСЦПВ другого авто видно без перемикання активного | S | — |
| Місячний бюджет з прогрес-баром | Паливо | Одне nullable-поле; прогрес проти totals.this_month + референс-лінія; projected_month_total уже є для порівняння | S | — |
| Порівняння витрати літо vs зима | Паливо | Пряме питання для дизеля (прогріви, DPF-регенерації), але треба ~рік заправок | S | ~Рік історії заправок |
| /паливо — статистика пального в боті | Telegram | «Скільки їсть і почому лив на WOG проти OKKO» з власних даних | S | — |
| /unlink у боті | Telegram | `service.unlink_chat` уже існує як мертвий код; симетрія з веб-DELETE | S | — |
| Препроцесинг зображень: deskew + adaptive threshold + upscale | OCR | Головний вбивця точності — фото термопаперу під кутом; локальна зміна в extract_text() під захистом наявних тестів | S | — |
| Мультифото-скан (багатосторінкові наряди, розвороти сервісної книжки) | OCR | Німецька сервісна книжка Golf сторінка за сторінкою; дрібниця після PDF-multi-page | S | PDF-підтримка |
| Manifest shortcuts («Заправка»/«Витрата») + iOS splash screens | PWA | Косметичний tier; роути вже існують | S | Іконки + manifest |
| Видалення акаунта + пропозиція завантажити дані перед цим | Дані/безпека | Каскади в моделях готові; має значення лише якщо інстанс колись хоститиме когось окрім власника | S | JSON-експорт |
| Consumption watchdog проти референсної смуги (Spritmonitor fleet avg для Golf 7 1.6 TDI) | Конкуренти | Ранній сигнал DPF/EGR/форсунок на 238-тис.-км дизелі; Telegram-алерт при дрейфі >~10% | S | — |
| httpOnly cookie auth (опція) | Дані/безпека | Прибирає XSS-поверхню localStorage, але тягне CSRF; реальна експозиція низька для single-user | M | — |

---

## Рекомендовані наступні ітерації

Порядок після затвердженої ітерації редагування. Перша — безальтернативно про безпеку даних: інцидент втрати вже був, і вся історія досі живе в одному файлі.

### Ітерація 1 — «Дані в безпеці»
**Мета:** жодна одинична подія (видалений файл, забутий пароль, злам) більше не може знищити історію авто.
- Автоматичний бекап БД + off-box копія в Telegram (P1/S) — з restore-drill
- Повний експорт/імпорт JSON + CSV з мапінгом Fuelio/Drivvo (P1/M)
- Скидання пароля через Telegram + change-password (P1/M)
- Rate limit на логін + відмова стартувати з дефолтним SECRET_KEY (P2/S)

### Ітерація 2 — «Щоденна заправка»
**Мета:** цикл заправки стає ~3 тапи і одразу винагороджує цифрою витрати; форма перестає втрачати і псувати дані.
- Onboarding першої заправки: CTA-плитки (P1/S)
- АЗС-чіпи + префіл останньої ціни (P1/S)
- Санітарні попередження одометр/дата (P1/S)
- Багаторядкові нотатки (P1/S)
- Inline л/100км на рядках заправок + toast (P1/M)
- Графік ціни за літр (P1/S)
- Категорії витрат + розбивка (P1/S)
- Фікс decimal-коми (P2/S)
- Хвіст, якщо лишиться час: PDF у /ocr/scan (P1/M) + збереження скана як вкладення (P1/S, ⏳ фото)

### Ітерація 3 — «Бот і сервіс без тертя»
**Мета:** усе логується з Telegram без відкриття PWA; завершення ТО — один тап, а не delete+recreate у двох місцях.
- One-tap «Виконано» /complete (P1/M, ⏳ Modal)
- Compliance-інтервали за датою: ОСЦПВ, техогляд, зелена карта (P1/S)
- Фікс avg_daily_km (P1/S)
- Підтвердження швидких витрат у боті (P1/S)
- Заправка текстом у боті (P1/M)
- Фото чека → OCR у боті (P1/M)
- Inline-кнопки «Виконано»/«Відкласти» на нагадуваннях (P1/S)
- Fallback-хендлери + set_my_commands (P2/S)

### Ітерація 4 — «Справжня PWA»
**Мета:** застосунок виглядає і поводиться як установлений нативний: живе без сигналу на заправці й не показує вчорашні цифри.
- Іконки + фікси manifest (P1/S)
- Офлайн-індикатор + NetworkFirst-кеш (P1/S)
- Офлайн-черга заправок + чернетка форми (P1/L)
- Клавіатурно-resume поліш (P2/S)

Далі природно проситься п'ята ітерація «Документи і дороги України» (бібліотека документів з expiry, VIN/номер, бренд-аналітика АЗС, фід цін) — цілком із P2.

---

## Повний реєстр пропозицій за доменами

Повний список з аудиту — нічого не втрачено. Формат: **назва** — чому \[пріоритет/зусилля\].

### Журнал (Logbook & Data Entry) — 11
1. **АЗС-чіпи + префіл останньої ціни** — щотижневе передруковування тих самих значень у найчастішій формі \[P1/S\]
2. **Санітарні попередження одометр/дата** — seeded-дані вже містять суперечність; захист full-to-full математики, warn-not-block \[P1/S\]
3. **Багаторядкові нотатки (textarea + clamp)** — реальні 200–380-символьні нотатки нечитабельні й болючі у вводі \[P1/S\]
4. **Inline л/100км на заправках + toast після збереження** — головна винагорода за логування для власника дизеля, зараз похована в аналітиці \[P1/M\]
5. **Undo після видалення** — кошик завжди видимий, видалення безповоротне \[P2/S, ⏳ Toast/ConfirmDialog\]
6. **«Зберегти і додати ще»** — бекфіл пачками без N навігаційних кіл \[P2/S\]
7. **Load-more + місячні заголовки** — стіна на 50 записах при готовому серверному offset \[P2/S\]
8. **Чіпи категорій витрат (ОСЦПВ, мийка, паркування, штраф, шини, страхування)** — витрати без структури; канонічні лейбли для пошуку й аналітики \[P2/S; злито з категоріями від конкурент-агента\]
9. **Фікс decimal-коми** — type="number" блокує кому, яку парсер уже розуміє \[P2/S\]
10. **OCR-прозорість: підсвітка автозаповнених полів + raw text** — рятувати частково невдалий скан оком, а не пересканом \[P3/M; злито з raw-text панеллю OCR-домену\]
11. **Персистентні кастомні пункти ТО** — «Mahle KX386» не має передруковуватись на кожному сервісі \[P3/S\]

### Гараж, профіль авто, інтервали — 8
1. **One-tap «Виконано» (POST /intervals/{id}/complete)** — транзакційно: запис ТО + просування інтервалу; вбиває delete+recreate \[P1/M, ⏳ Modal\]
2. **Compliance-інтервали за датою (ОСЦПВ, техогляд, зелена карта)** — схема вже вміє days-only; штрафні дедлайни їдуть існуючими нагадуваннями \[P1/S\]
3. **Фікс avg_daily_km (вікно + override + пояснення)** — lifetime-середнє з 2016 бреше прогнозам до 2× \[P1/S\]
4. **Серверні fuel-type-aware пресети з дедупом** — дизель/бензин/ГБО-набори, ідемпотентність \[P2/M\]
5. **VIN + номерний знак** — щотижнева потреба для ЄС-імпорту; номер у PDF-звіті \[P2/S\]
6. **Шини: сезонні date-інтервали + нотатки про комплект** — зимова заміна за ~3 місяці \[P2/S\]
7. **Telegram-кнопка «Виконано» на нагадуваннях** — закриває цикл там, де власник дізнається про роботу \[P2/S; злито з inline-кнопками telegram-домену\]
8. **Статус-крапка найгіршого інтервалу на картці авто** — стан другого авто без перемикання \[P3/S\]

### Паливо, витрата, аналітика — 9
1. **Onboarding першої заправки (CTA-плитки)** — 0 заправок у БД; перше, що відчує власник \[P1/S\]
2. **Графік ціни за літр** — дані вже є на кожній заправці \[P1/S\]
3. **fuel_type на заправці (ГБО-ready)** — міграція тривіальна зараз, болюча потім \[P2/M\]
4. **Автокомпліт АЗС + середня ціна по станціях** — фікс дрейфу вільного тексту в місці вводу \[P2/S; злито з бренд-аналітикою конкурент-агента\]
5. **Км/місяць + ₴/км по місяцях** — одометр уже в кожному записі; чесна назва плитки \[P2/M\]
6. **Об'єм бака + запас ходу** — «≈ 900 км на баку» + перехоплення одруків \[P2/S\]
7. **Місячний бюджет із прогресом** — projected_month_total уже є для порівняння \[P3/S\]
8. **Літо vs зима** — прогріви/DPF-регенерації; треба ~рік історії \[P3/S\]
9. **CSV-експорт паливної історії** — «подивитись у таблиці» без нових графіків \[P3/S; злито в загальний експорт/імпорт\]

### Telegram-бот — 9
1. **Підтвердження перед збереженням швидкої витрати** — «240 054» більше не стає сміттєвою витратою \[P1/S\]
2. **Заправка текстом («заправка 1800 41.5л»)** — найчастіша подія, недоступна боту; живить двигун витрати \[P1/M\]
3. **Фото чека → OCR → confirm-картка** — на заправці відкритий Telegram, а не PWA; tesseract у to_thread, webhook не потрібен \[P1/M\]
4. **Inline «Виконано»/«Відкласти 30 дн.» на нагадуваннях** — кінець вічного 7-денного циклу простроченої гальмівної рідини \[P1/S\]
5. **/report — PDF документом** — на СТО чи при продажу \[P2/S\]
6. **Тижневий дайджест + фіксована година Києва** — заодно лагодить дрейф розкладу після деплоїв \[P2/M\]
7. **Fallback-хендлери + set_my_commands** — кінець мовчання на фото/голос і провалів у парсер витрат \[P2/S\]
8. **/паливо — паливна статистика** — з власних даних, без зовнішніх API \[P3/S\]
9. **/unlink** — unlink_chat уже існує як мертвий код \[P3/S\]

### OCR та документи — 8
1. **PDF у /ocr/scan (pypdf text layer + растровий fallback)** — реальні PDF-рахунки сьогодні отримують 415 \[P1/M\]
2. **Збереження скана як вкладення + raw_text із записом** — одне фото на обидві роботи; аудитованість \[P1/S, ⏳ фото + сторінка запису\]
3. **Raw-text панель + збір виправлень** — самонарощуваний регресійний корпус для парсера \[P2/S\]
4. **Парсер друкованих накладних/нарядів** — найбільша економія клавіш на подію; чесний скоуп: тільки друковане \[P2/M, після PDF\]
5. **Бібліотека документів з expiry → date-інтервали** — ОСЦПВ/техпаспорт/зелена карта з нагадуваннями \[P2/M, ⏳ фото; злито з документами конкурент-агента\]
6. **Opt-in LLM-vision fallback** — рукопис поза tesseract; env-gated, копійки на обсягах одного користувача \[P2/M\]
7. **Препроцесинг: deskew + threshold + upscale** — дешевий приріст точності на фото термопаперу \[P3/S\]
8. **Мультифото-скан** — сервісна книжка розворотами \[P3/S, після PDF\]

### PWA, мобільний UX, onboarding — 8
1. **Іконки + фікси manifest** — зламана iOS-плитка щодня перед очима; lang: uk \[P1/S\]
2. **Офлайн-черга заправок + чернетка (Dexie/IndexedDB)** — заправка без сигналу; скоуп лише створення записів \[P1/L; злито з offline-queue конкурент-агента\]
3. **Офлайн-індикатор + NetworkFirst-кеш GET /api** — одногодинний win, read-only офлайн \[P1/S\]
4. **Світла тема (auto/manual)** — читабельність на сонці біля колонки \[P2/M\]
5. **Onboarding-чекліст на Dashboard** — пресети/Telegram/перша заправка стають видимими \[P2/S\]
6. **Клавіатурно-resume поліш** — enterkeyhint, autofocus, refetch-on-focus, toast оновлення SW \[P2/S\]
7. **Pull-to-refresh** — у standalone нема кнопки reload \[P2/M\]
8. **Manifest shortcuts + iOS splash** — довгий тап → «Заправка»/«Витрата» \[P3/S, після іконок\]

### Дані, акаунт, платформа — 9
1. **Автобекап з ротацією + off-box копія через бота** — інцидент втрати вже був; найвищий реальний ризик у всьому аудиті \[P1/S\]
2. **JSON-експорт/імпорт усього акаунта (+ CSV на авто)** — бекап + data ownership + міграція SQLite→Postgres \[P1/M; злито з CSV конкурент-агента\]
3. **Скидання пароля через Telegram + change-password** — другий шлях тотальної втрати; вся інфраструктура вже існує \[P1/M\]
4. **Rate limit логіну + refuse-to-boot на дефолтному SECRET_KEY** — перед виходом в інтернет \[P2/S\]
5. **VPS-runbook + compose.prod з Caddy HTTPS** — PWA без HTTPS не встановлюється; деплой зараз зламаний \[P2/M, після бекапу\]
6. **REGISTRATION_ENABLED (off у prod)** — чужі реєстрації на персональному інстансі \[P2/S\]
7. **Session sanity: 30 днів + token_version** — примирення 43200-хв проти 60-хв конфігів + kill switch \[P2/M\]
8. **httpOnly cookie auth (опція)** — прибирає XSS-поверхню, тягне CSRF; не цього місяця \[P3/M\]
9. **Видалення акаунта + data-download** — чиста громадянськість \[P3/S, після експорту\]

### Конкурентний ландшафт — 8
1. **CSV експорт + імпорт (мапінг Fuelio/Drivvo)** — єдина table-stakes фіча всіх шести, якої нема \[P1/M; злито з JSON-експортом\]
2. **Категорії витрат + розбивка** — навіть безкоштовний Drivvo це має; відчується за дні через бот \[P1/S; злито з чіпами журналу\]
3. **Документи авто з expiry (ОСЦПВ, зелена карта, техогляд)** — український killer: штраф + особиста відповідальність \[P2/M, ⏳ фото; злито з бібліотекою документів\]
4. **Бренд-аналітика АЗС з наявного gas_station** — headline-фіча Simply Auto майже задарма \[P2/S; злито з автокомплітом\]
5. **Щоденний фід цін UA + «ви заплатили vs ринок»** — рідкісна фіча, зроблена локально \[P2/M\]
6. **Офлайн-черга записів у PWA** — усі мобільні конкуренти offline-first \[P2/M; злито з чергою PWA-домену\]
7. **Consumption watchdog проти референсу** — «бідняцький Spritmonitor»: ранній сигнал DPF/EGR \[P3/S\]
8. **fuel_type на заправці (ГБО)** — Fuelio має bi-fuel; конкурент-агент радить відкласти до появи ГБО-авто \[P3/M; злито, конфлікт пріоритету з паливним доменом\]

---

### Зафіксовані розбіжності між агентами

1. **fuel_type на заправці**: паливний агент — P2 («мігруй зараз, поки історія порожня»), конкурентний — P3 («відклади, поки в гаражі нема ГБО-авто»). У беклозі — P2 з приміткою: аргумент про дешевизну міграції до накопичення даних сильніший.
2. **Фід цін на пальне**: конкурентний агент пропонує щоденний скрейп (P2/M), telegram-агент прямо пише «живі ринкові ціни потребують скрейпінгу і не варті того для одного користувача». У беклозі лишено як P2 з позначкою конфлікту — рішення за власником.
3. **Кнопки на нагадуваннях**: telegram-агент — P1 (простий штамп дат), garage-агент — P2 (повна транзакційна версія через /complete). Злито в P1/S зі сходинкою до /complete.
4. **Офлайн-черга**: PWA-агент — P1/L (з чернеткою форми), конкурентний — P2/M (лише background sync). Злито в P1/L за детальнішою специфікацією PWA-агента.
5. **Категорії витрат**: журнальний агент — чіпи без зміни схеми (P2/S), конкурентний — повноцінна міграція category (P1/S). Злито в P1/S: чіпи як UI поверх реальної колонки.
